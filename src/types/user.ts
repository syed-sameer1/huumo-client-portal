export enum ClientStatus {
  pending = 'pending',

  active = 'active',

  inActive = 'inActive',
}

export enum UserStatus {
  pending = 'pending',

  active = 'active',

  inActive = 'inActive',
}

export enum UserRole {
  owner = 'owner',

  admin = 'admin',

  member = 'member',
}
